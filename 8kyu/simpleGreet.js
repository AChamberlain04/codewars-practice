//Create a function that accepts a paramenter name and returns a greeting in the form of "Hello, <name> how are you doing today?".
function greet(name) {
  return `Hello, ${name} how are you doing today?`;
}

// or
const greet = name => `Hello, ${name} how are you doing today?`;